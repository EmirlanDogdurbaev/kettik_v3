import Layout from "../../components/Layout/Layout";
import cl from "./Tour.module.scss";

const Tour = () => {
  return (
    <section className={cl.Tour}>
      <Layout />
    </section>
  );
};

export default Tour;
